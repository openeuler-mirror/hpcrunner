[SERVER]
11.11.11.11

[DOWNLOAD]
gperftools/2.15 https://github.com/gperftools/gperftools/archive/refs/tags/gperftools-2.15.tar.gz gperftools-2.15.tar.gz


[DEPENDENCY]
set -e
set -x

yum install make cmake -y

./jarvis -install hpckit/${HPCKIT_VERSION} any

source ${JARVIS_ROOT}/software/utils/hpckit/${HPCKIT_VERSION}/HPCKit/${HPCKIT_VERSION}/setvars.sh

mkdir -p ${JARVIS_ROOT}/software/apps
tar -xvf ${JARVIS_DOWNLOAD}/gperftools-2.15.tar.gz -C ${JARVIS_ROOT}/software/apps

[ENV]
source ${JARVIS_ROOT}/software/utils/hpckit/${HPCKIT_VERSION}/HPCKit/${HPCKIT_VERSION}/setvars.sh
export PATH=${JARVIS_ROOT}/gperftools-2.15/bin:$PATH
export CC=`which gcc`
export CXX=`which g++`
export FC=`which gfortran`

[APP]
app_name = pprof
build_dir = ${JARVIS_ROOT}/software/apps/gperftools-gperftools-2.15
binary_dir = ${JARVIS_ROOT}/gperftools-2.15/bin
case_dir =

[BUILD]
./autogen.sh
./configure --prefix=${JARVIS_ROOT}/gperftools-2.15
make -j
make -j install

[CLEAN]
make clean

[RUN]
run =
binary =
nodes = 1
