[SERVER]
11.11.11.11

[DOWNLOAD]

[DEPENDENCY]
module purge
./jarvis -install hpckit/${HPCKIT_VERSION} any
module use ${JARVIS_ROOT}/software/modulefiles
GCC_VERSION=`ls software/utils/hpckit/${HPCKIT_VERSION}/HPCKit/${HPCKIT_VERSION}/modulefiles/gcc|grep compiler|awk -F "compiler" '{print $2}'`
module load hpckit${HPCKIT_VERSION}/gcc/compiler${GCC_VERSION}/gccmodule  hpckit${HPCKIT_VERSION}/gcc/hmpi${HMPI_VERSION}/release

[ENV]
module purge
module use ${JARVIS_ROOT}/software/modulefiles
GCC_VERSION=`ls software/utils/hpckit/${HPCKIT_VERSION}/HPCKit/${HPCKIT_VERSION}/modulefiles/gcc|grep compiler|awk -F "compiler" '{print $2}'`
module load hpckit${HPCKIT_VERSION}/gcc/compiler${GCC_VERSION}/gccmodule  hpckit${HPCKIT_VERSION}/gcc/hmpi${HMPI_VERSION}/release

[APP]
app_name = optimized-routines
build_dir = ${JARVIS_ROOT}
binary_dir =
case_dir = ${JARVIS_TMP}/optimized-routines-20.02

[BUILD]
./jarvis -install optimized-routines/20.02 gcc+mpi

[RUN]
run = /root/hpcrunner/software/libs/gcc12.3.1-hmpi25.1.0/optimized-routines/20.02/bin/runulp.sh
binary =
nodes = 1

