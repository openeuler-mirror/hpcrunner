[SERVER]
11.11.11.11

[DOWNLOAD]
kraken2/2.1.2 https://github.com/DerrickWood/kraken2/archive/refs/tags/v2.1.2.tar.gz kraken2-2.1.2.tar.gz

[DEPENDENCY]
set -e
set -x
./jarvis -install hpckit/${HPCKIT_VERSION} any
module purge
module use ${JARVIS_ROOT}/software/modulefiles
module load hpckit${HPCKIT_VERSION}/bisheng/compiler${BISHENG_VERSION}/bishengmodule

cd ${JARVIS_TMP}
rm -rf kraken2-2.1.2
tar -xvf ${JARVIS_DOWNLOAD}/kraken2-2.1.2.tar.gz

[ENV]
module purge
module use ${JARVIS_ROOT}/software/utils/hpckit/${HPCKIT_VERSION}/HPCKit/${HPCKIT_VERSION}/modulefiles
module load bisheng/compiler${BISHENG_VERSION}/bishengmodule


[APP]
app_name = kraken2
build_dir = ${JARVIS_TMP}/kraken2-2.1.2
binary_dir = ${JARVIS_ROOT}/software/apps/bisheng${BISHENG_VERSION}/kraken2/2.1.2
case_dir = ${JARVIS_TMP/kraken2-2.1.2

[BUILD]
cp src/Makefile src/Makefile.bak
sed -i '1s/^CXX *= *g++/CXX = clang++/' src/Makefile
sed -i '2{/[^ ]-fsigned-char/!s/$/ -fsigned-char/}' src/Makefile
sh install_kraken2.sh ${JARVIS_ROOT}/software/apps/bisheng${BISHENG_VERSION}/kraken2/2.1.2

[CLEAN]
rm -rf ${JARVIS_ROOT}/software/apps/bisheng${BISHENG_VERSION}/kraken2/2.1.2

[RUN]
run = 
binary = kraken2 -h
nodes = 1

[PERF]
perf= 
nsys=
ncu=--target-processes all
