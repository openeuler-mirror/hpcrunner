[SERVER]
11.11.11.11

[DOWNLOAD]
augustus/3.3.3 https://github.com/Gaius-Augustus/Augustus/archive/v3.3.3.tar.gz Augustus-3.3.3.tar.gz

[DEPENDENCY]
set -e
set -x

module purge
module use $JARVIS_MODULES/modules
./jarvis -install hpckit/2025.1.0.SPC001 any
module load hpckit/2025.1.0.SPC001
module load bisheng/compiler4.2.0.2/bishengmodule


echo "done"

[ENV]
module purge
module use $JARVIS_MODULES/modules
module load hpckit/2025.1.0.SPC001
module load bisheng/compiler4.2.0.2/bishengmodule


[APP]
fake = 0
app_name = augustus
app_version = 3.3.3
compiler = bisheng
build_dir = ${JARVIS_APP}/augustus/3.3.3-%#MYKBSVER#%/project
binary_dir = 
case_dir = ${JARVIS_JOBSCRIPT}/augustus/3.3.3

[BUILD]
set -e
source ${JARVIS_LIBSHELL}
set -x
key=$2
bmod="${key#*=}"
BUILD_DIR=$(pwd)
PATH_INSTALL=$1
echo "CURRENT SHELL:$SHELL"
####### build prepare
set +x && echo_info "build prepare!!!" && set -x
####### build start
set +x && echo_info "build start" && set -x
set +x && build_mod_check "${BUILD_DIR}/Augustus-3.3.3/bin/augustus" "prompt" && set -x
if true; then
    rm -rf ${BUILD_DIR}/Augustus-3.3.3
    tar --no-same-owner -zxvf ${JARVIS_DOWNLOAD}/Augustus-3.3.3.tar.gz -C ${BUILD_DIR}
    pip3 install parallel_augustus -i https://mirrors.aliyun.com/pypi/simple --trusted-host mirrors.aliyun.com
    rm -rf ${BUILD_DIR}/bin
    mkdir ${BUILD_DIR}/bin
    cp ${JARVIS_DOWNLOAD}/augustus ${BUILD_DIR}/bin
    chmod +x ${BUILD_DIR}/bin/augustus
    #${DOWNLOAD_TOOL} -u http://bioinf.uni-greifswald.de/augustus/datasets/sag178.fa -f sag178.fa
fi
####### build done
set +x && echo_info "build done!!!" && set -x
####### Install jobscript
set +x && echo_info "Install jobscript!!!" && set -x
JOBDIR=${JARVIS_JOBSCRIPT}/augustus/3.3.3
mkdir -p ${JOBDIR}
${BUILD_DIR}/env.sh_gen.sh ${JOBDIR}/env.sh
echo "module load augustus/3.3.3-%#MYKBSVER#%" >> ${JOBDIR}/env.sh
cp ${JARVIS_DOWNLOAD}/sag178.fa ${JOBDIR}/ -ar
cat > ${JOBDIR}/job_run.sh << EOF
#!/bin/bash
#DSUB -n case_wrf
#DSUB --job_type cosched:hmpi
#DSUB -N 1
#DSUB -nn 1
#DSUB -rpn 1
#DSUB -R "cpu=256"
#DSUB -o pytorch_out_%J.log
#DSUB -e pytorch_err_%J.log
#DSUB -T '2h'
export HOSTFILE=hostfile.wrf
rm -f \$HOSTFILE
touch \$HOSTFILE
cat \${CCSCHEDULER_ALLOC_FILE} | sort > \$HOSTFILE
export OMPI_ALLOW_RUN_AS_ROOT=1
export OMPI_ALLOW_RUN_AS_ROOT_CONFIRM=1
source ./env.sh
export AUGUSTUS_CONFIG_PATH=${BUILD_DIR}/Augustus-3.3.3/config
cd job_work
rm -rf sag178.res
{ time -p numactl -C 0-42 parallel_augustus -g sag178.fa -o sag178.res -c 1024 -p 128 --extra '--species=human --UTR=on';  } 2>&1 | tee tee_jax.xlog
EOF
cat >${JOBDIR}/local_run.sh << EOF
#!/bin/bash
source ./env.sh
export OMPI_ALLOW_RUN_AS_ROOT=1
export OMPI_ALLOW_RUN_AS_ROOT_CONFIRM=1
cd local_work
rm -rf sag178.res
{ time -p numactl -C 0-42 parallel_augustus -g sag178.fa -o sag178.res -c 1024 -p 128 --extra '--species=human --UTR=on'  ; } 2>&1 | tee tee_out.xlog
EOF
cat > ${JOBDIR}/augustus-3.3.3-run.sh << EOF
#!/bin/bash
source env.sh
export AUGUSTUS_CONFIG_PATH=${BUILD_DIR}/Augustus-3.3.3/config
mkdir local_work job_work -p
./local_run.sh 2>&1 | tee tee_local_run.xlog
dsub -s job_run.sh
EOF
chmod u+x ${JOBDIR}/env.sh
chmod u+x ${JOBDIR}/job_run.sh
chmod u+x ${JOBDIR}/local_run.sh
chmod u+x ${JOBDIR}/augustus-3.3.3-run.sh


[RUN]
