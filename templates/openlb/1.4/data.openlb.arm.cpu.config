[SERVER]
11.11.11.11

[DOWNLOAD]
openlb/1.4 https://www.openlb.net/wp-content/uploads/2020/11/olb-1.4r0.tgz

[DEPENDENCY]
set -e
set -x
sudo yum install gnuplot -y
module purge
./jarvis -install hpckit/${HPCKIT_VERSION} any
module use ${JARVIS_ROOT}/software/modulefiles
module load hpckit${HPCKIT_VERSION}/bisheng/compiler${BISHENG_VERSION}/bishengmodule
module load hpckit${HPCKIT_VERSION}/bisheng/hmpi${HMPI_VERSION}/release
export CC=`which clang`
export CXX=`which clang++`
export FC=`which flang`
rm -rf ${JARVIS_ROOT}/software/apps/bisheng${BISHENG_VERSION}-hmpi${HMPI_VERSION}/openlb/1.4/olb-1.4r0
mkdir -p ${JARVIS_ROOT}/software/apps/bisheng${BISHENG_VERSION}-hmpi${HMPI_VERSION}/openlb/1.4
cd ${JARVIS_ROOT}/software/apps/bisheng${BISHENG_VERSION}-hmpi${HMPI_VERSION}/openlb/1.4
tar -xvf ${JARVIS_DOWNLOAD}/olb-1.4r0.tgz

[ENV]
module purge
module use ${JARVIS_ROOT}/software/modulefiles
module load hpckit${HPCKIT_VERSION}/bisheng/compiler${BISHENG_VERSION}/bishengmodule
module load hpckit${HPCKIT_VERSION}/bisheng/hmpi${HMPI_VERSION}/release
export CC=`which clang`
export CXX=`which clang++`
export FC=`which flang`

[APP]
app_name = openlb
build_dir = ${JARVIS_ROOT}/software/apps/bisheng${BISHENG_VERSION}-hmpi${HMPI_VERSION}/openlb/1.4/olb-1.4r0
binary_dir =
case_dir = ${JARVIS_ROOT}/software/apps/bisheng${BISHENG_VERSION}-hmpi${HMPI_VERSION}/openlb/1.4/olb-1.4r0/examples/laminar/cylinder2d

[BUILD]
patch -p0 config.mk ${JARVIS_ROOT}/tunning/openlb/hmpi-bisheng-default-hybrid.patch
make samples -j

[CLEAN]
make clean

[RUN]
run = make && ./cylinder2d
binary =
nodes = 1
